<body id="reviewShow">
  <div class="container backbox">
    <div class="row">
      <div class="col-4">
        <%= image_tag("#{Faker::LoremFlickr.image(search_terms: ["#{@product.name}"])}", width: "100px", class: "productPic") %>
      </div>
      <div class="col-6">
        <h1><%= @product.name %></h1>
        <br>
        <h3><%= @product.country_of_origin %><br><%= @review.rating %> out of 5 Stars</h3>
      </div>
      <h3 id="reviewText">"<%= @review.content_body %>" <br><span id="authorCredit">- <%= @review.author %></span></h3>
    </div>
    <div class="row">
      <div class="col-4">
    <p><%= link_to "Edit review", edit_product_review_path(@product, @review) %></p>
      </div>
      <div class="col-4">
    <p><%= link_to "Delete review", product_review_path(@product, @review), :data => {:confirm => "Are you sure?", :method => "delete"} %></p>
      </div>
      <div class="col-4">
    <p><%= link_to "Return to products", product_path(@product) %></p>
      </div>
    </div>
